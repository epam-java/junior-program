package com.epam.jmp.patterns.iterator;

import static com.epam.jmp.patterns.iterator.Range.range;

/*
    Необходимо реализовать функцию range, генерирующую последовательность чисел в заданном диапазоне.
    Генерация чисел должна быть ленивая. Т.е. функция не должна возвращать список (массив),
    числа должны возвращаться по одному каждый раз, когда они нужны.
 */
public class Main {

    public static void main(String[] args) {
        /*
        for (int i : range(1, 10)) {
            System.out.println(i);
        }
        */
    }
}
